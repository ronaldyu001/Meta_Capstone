# imports
import mysql.connector as connector
from mysql.connector import Error
from utility_funcs import create_cursor



def bookings( connection ):
    cursor = create_cursor( connection )
    


def courses( connection ):
    cursor = create_cursor( connection )


def customers( connection ):
    cursor = create_cursor( connection )


def deliveries( connection ):
    cursor = create_cursor( connection )


def desserts( connection ):
    cursor = create_cursor( connection )


def drinks( connection ):
    cursor = create_cursor( connection )


def menus( connection ):
    cursor = create_cursor( connection )


def orders( connection ):
    cursor = create_cursor( connection )


def sides( connection ):
    cursor = create_cursor( connection )


def staff( connection ):
    cursor = create_cursor( connection )


def starters( connection ):
    cursor = create_cursor( connection )


def tables( connection ):
    cursor = create_cursor( connection )
